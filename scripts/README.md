# Scripts

This folder contains reproducible scripts for exploratory data analysis (EDA).

- `eda.py`: runs a non-interactive EDA pipeline that writes summary CSVs to `outputs/`.

Usage:

```bash
python scripts/eda.py
```
